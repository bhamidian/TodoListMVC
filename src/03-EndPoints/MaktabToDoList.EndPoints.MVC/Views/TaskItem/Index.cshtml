@using System.Globalization
@using MaktabToDoList.Domain.Core.Task.Enums
@model MaktabToDoList.EndPoints.MVC.Models.ViewModels.GetAllTaskItemsViewModel
@{
    Layout = null;
    var persian = new PersianCalendar();
    var now = DateTime.Now;
    string weekdayFa = now.DayOfWeek switch
    {
        DayOfWeek.Saturday => "شنبه",
        DayOfWeek.Sunday => "یک‌شنبه",
        DayOfWeek.Monday => "دوشنبه",
        DayOfWeek.Tuesday => "سه‌شنبه",
        DayOfWeek.Wednesday => "چهارشنبه",
        DayOfWeek.Thursday => "پنج‌شنبه",
        DayOfWeek.Friday => "جمعه",
        _ => ""
    };
    string todayFa = $"{persian.GetYear(now)}/{persian.GetMonth(now):00}/{persian.GetDayOfMonth(now):00}";
    string currentTime = now.ToString("HH:mm");
}
<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>مدیریت تسک‌ها</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: "IRANSans", sans-serif;
        }

        table th, table td {
            text-align: center;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-cyan-50 to-gray-50 min-h-screen p-8">

    <div class="flex justify-between items-center mb-8">

        <div class="flex gap-3">
            <span class="bg-purple-100 text-purple-700 px-4 py-1 rounded-full text-sm font-medium">Week 19</span>
            <span class="bg-purple-100 text-purple-700 px-4 py-1 rounded-full text-sm font-medium">CW</span>
            <span class="bg-purple-100 text-purple-700 px-4 py-1 rounded-full text-sm font-medium">مکتب 135</span>
            <a asp-action="Login" asp-controller="Account"
               class="bg-red-600 hover:bg-red-500 text-white px-5 py-2 rounded-lg shadow-md transition hover:scale-105">
                خروج
            </a>
        </div>
    </div>

    <form asp-action="GetAllByFilters" asp-controller="TaskItem" method="get" class="bg-white shadow-md rounded-xl p-4 mb-6 border border-gray-200">
        <div class="flex flex-wrap justify-between items-center gap-4">
            <div class="flex items-center gap-2">
                <input type="text" name="search" value="@Context.Request.Query["search"]"
                       placeholder="جستجو در عنوان و دسته‌بندی..."
                       class="border border-gray-300 rounded-lg px-3 py-2 w-64 shadow-sm focus:ring-purple-500 focus:border-purple-500">
            </div>

            <div class="flex flex-wrap items-center gap-3">
                <select name="sort" class="border border-gray-300 rounded-lg px-3 py-2 shadow-sm focus:ring-purple-500 focus:border-purple-500">
                    <option value="">مرتب‌سازی</option>
                    <option value="title-asc" selected="@(Context.Request.Query["sort"] == "title-asc")">عنوان (الف → ی)</option>
                    <option value="title-desc" selected="@(Context.Request.Query["sort"] == "title-desc")">عنوان (ی → الف)</option>
                    <option value="due-asc" selected="@(Context.Request.Query["sort"] == "due-asc")">سررسید (جدید → قدیم)</option>
                    <option value="due-desc" selected="@(Context.Request.Query["sort"] == "due-desc")">سررسید (قدیم → جدید)</option>
                    <option value="status-done" selected="@(Context.Request.Query["sort"] == "status-done")">انجام‌شده اول</option>
                    <option value="status-notdone" selected="@(Context.Request.Query["sort"] == "status-notdone")">انجام‌نشده اول</option>
                </select>

                <select name="status" class="border border-gray-300 rounded-lg px-3 py-2 shadow-sm focus:ring-purple-500 focus:border-purple-500">
                    <option value="">همه وضعیت‌ها</option>
                    <option value="0" selected="@(Context.Request.Query["status"] == "0")">در حال انجام</option>
                    <option value="1" selected="@(Context.Request.Query["status"] == "1")">انجام شده</option>
                    <option value="2" selected="@(Context.Request.Query["status"] == "2")">انجام نشده</option>
                </select>

                <input type="date" name="dueDate" value="@Context.Request.Query["dueDate"]"
                       class="border border-gray-300 rounded-lg px-3 py-2 shadow-sm focus:ring-purple-500 focus:border-purple-500" />

                <button type="submit"
                        class="bg-purple-600 hover:bg-purple-500 text-white font-bold px-6 py-2 rounded-lg shadow-md transition transform hover:scale-105">
                    اعمال فیلتر
                </button>
            </div>

            <a asp-action="Create" asp-controller="TaskItem"
               class="bg-cyan-600 hover:bg-cyan-500 text-white font-bold px-6 py-2 rounded-lg shadow-md transition transform hover:scale-105">
                افزودن تسک
            </a>
        </div>
    </form>

    <div class="flex justify-end mb-6 text-gray-700 font-semibold text-lg">
        امروز @weekdayFa — @todayFa ساعت @currentTime
    </div>

    <div class="overflow-x-auto bg-white rounded-xl shadow-lg border border-gray-200 hover:shadow-xl transition-shadow duration-300">
        <table class="min-w-full text-sm text-gray-700">
            <thead class="bg-purple-100 text-purple-700 font-semibold">
                <tr>
                    <th class="px-4 py-3">شناسه</th>
                    <th class="px-4 py-3">موضوع</th>
                    <th class="px-4 py-3">تاریخ شروع</th>
                    <th class="px-4 py-3">تاریخ اتمام</th>
                    <th class="px-4 py-3">دسته‌بندی</th>
                    <th class="px-4 py-3">وضعیت</th>
                    <th class="px-4 py-3" colspan="2">عملیات</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.GetAllTasks)
                {
                    string rowClass = item.TaskItemStatus switch
                    {
                        TaskItemStatus.Doing => "bg-blue-100 text-blue-800",
                        TaskItemStatus.Done => "bg-green-100 text-green-800",
                        TaskItemStatus.NotDone => "bg-red-100 text-red-800",
                        _ => "bg-gray-50"
                    };
                    <tr class="border-t hover:bg-gray-50 transition @rowClass">
                        <td class="px-4 py-3">@item.Id</td>
                        <td class="px-4 py-3">@item.Title</td>
                        <td class="px-4 py-3">@item.StartDateFa</td>
                        <td class="px-4 py-3">@item.EndDateFa</td>
                        <td class="px-4 py-3">@item.CategoryName</td>
                        <td class="px-4 py-3">
                            <form asp-action="UpdateStatus" asp-controller="TaskItem" method="post" class="inline">
                                <input type="hidden" name="taskId" value="@item.Id" />
                                <select name="statusId" class="border rounded-lg px-3 py-1 focus:ring-purple-500 focus:border-purple-500" onchange="this.form.submit()">
                                    <option value="0" selected="@((int)item.TaskItemStatus == 0)">در حال انجام</option>
                                    <option value="1" selected="@((int)item.TaskItemStatus == 1)">انجام شده</option>
                                    <option value="2" selected="@((int)item.TaskItemStatus == 2)">انجام نشده</option>
                                </select>
                            </form>
                        </td>
                        <td class="px-4 py-3">
                            <form asp-action="Update" asp-controller="TaskItem" method="post">
                                <input type="hidden" name="id" value="@item.Id" />
                                <button class="bg-yellow-400 hover:bg-yellow-300 transition px-5 py-2 rounded-lg text-white font-semibold shadow-sm">ویرایش</button>
                            </form>
                        </td>
                        <td class="px-4 py-3">
                            <form asp-action="Delete" asp-controller="TaskItem" method="post">
                                <input type="hidden" name="id" value="@item.Id" />
                                <button type="submit" class="bg-red-500 hover:bg-red-400 transition px-5 py-2 rounded-lg text-white font-semibold shadow-sm">حذف</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>